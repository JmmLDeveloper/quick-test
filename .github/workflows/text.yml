name: Testo Siniestro
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
jobs:
  do-stuff:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ secrets.AAA }}
      - run: echo ${{ secrets.BBB }}
      - run: echo ${{ secrets.CCC }}
      - run: ssh 


